import { UserEntity } from './User';
import { ExampleEntity } from './Example';

export const ALL_ENTITIES = Object.freeze([
  UserEntity,
  ExampleEntity,
]);

export {
  UserEntity,
  ExampleEntity,
};
